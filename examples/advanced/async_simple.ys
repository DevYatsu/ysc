// Basic async/await demonstration
// async functions compile to regular functions for now
async fun greet(name) {
    ret "Hello, " + name
}

// Calling an async function
msg = greet("Yatsu")
print(msg)

// Await on a value passes through directly
result = await greet("World")
print("Greeting: " + result)

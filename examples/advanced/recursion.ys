// Recursion

fun fib(n) {
  if n < 2 {
    ret n
  } else {
    ret fib(n - 1) + fib(n - 2)
  }
}

print("fib(10) = " + str(fib(10)))

// Direct recursion
fun factorial(n) {
  if n <= 1 {
    ret 1
  } else {
    ret n * factorial(n - 1)
  }
}

print("5! = " + str(factorial(5)))
print("10! = " + str(factorial(10)))
